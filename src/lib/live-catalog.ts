/**
 * Fixture catalog plus the local seller overlay.
 * Import `@/lib/commerce` for shapes. These aliases are for pages
 * that want to say “live” out loud — shop lookups already merge.
 */
export {
  allListings as liveListings,
  listingById as liveListingById,
  listingsByHub as liveListingsByHub,
  listingsByStall as liveListingsByStall,
  listingsByType as liveListingsByType,
  stallById as liveStallById,
  stallBySlug as liveStallBySlug,
} from "@/lib/commerce";
